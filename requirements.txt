# HOVE Risk-Control Linker environment.
# Python: 3.11
#
# The public repository ships a research checkpoint, public config, aggregate
# metrics, figures, and release checks. It does not include private rows or
# concept-memory artifacts, but training/inference adapters still need the core
# HOVE scientific and neural runtime.

# Public release audits and tests.
pytest==9.0.3

# Scientific/data runtime for aggregate evaluation and adapter development.
numpy==2.1.2
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
joblib==1.4.2
networkx==3.3
PyYAML==6.0.3
tqdm==4.67.1
matplotlib==3.10.8
pillow==11.0.0

# Neural checkpoint loading, retrieval features, training, and inference.
torch==2.10.0
transformers==5.5.4
huggingface-hub==1.1.2
tokenizers==0.22.1
safetensors==0.6.2
sentence-transformers==5.1.2
accelerate==1.11.0
pytorch-lightning==2.6.0
torchmetrics==1.8.2
hydra-core==1.3.2
omegaconf==2.3.0
